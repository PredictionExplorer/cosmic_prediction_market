/**
 * Motion's DOM animation feature bundle, isolated in its own module so
 * bundlers emit it as a separate async chunk (see LazyMotion in providers).
 */
export { domAnimation as default } from "motion/react";
